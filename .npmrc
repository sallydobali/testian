# npm configuration file
# Avoid audit warnings
audit=false
fund=false

# Cache settings
cache=.npm-cache

# Registry settings
registry=https://registry.npmjs.org/

# Avoid permission issues
unsafe-perm=true
